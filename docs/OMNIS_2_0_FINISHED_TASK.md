# Omnis 2.0 — Build Progress Tracker

> **Status:** Live build tracker for the Omnis 2.0 implementation.
> This document tracks what has been built, what is in progress, and what
> remains. It is the single source of truth for build progress.
>
> **References (read-only, never edited):**
> 1. [OMNIS_2_0_SPEC.md](OMNIS_2_0_SPEC.md) — product specification
> 2. [OMNIS_2_0_UI_SPEC.md](OMNIS_2_0_UI_SPEC.md) — UI/UX specification
> 3. [OMNIS_2_0_PLUGINS.md](OMNIS_2_0_PLUGINS.md) — plugin architecture & developer guide

---

## Phase 1 — Reliability

| # | Item | Status |
|---|------|--------|
| 1 | Playback engine | 🟡 In progress — `AudioEngine` covers play/pause/seek/next/prev/gapless/crossfade/speed/pitch/skip-silence/shuffle/repeat/volume. Started §51.2's split (OS media-session integration → `playback_os_integration.dart`; A-B repeat → a decoupled, unit-tested `AbRepeatController`), shrinking the file from 1208 to ~880 lines. Also introduced `PlaybackEngine` (§51.3 capability interface) — `AudioEngine` now `implements` it, and `PlaybackWatchdog`/`PlaybackRecovery` depend on the interface, not the concrete class. Still owes the bigger `QueueController`/`OutputController`/`AudioSessionController` split — queue mutation and the crossfade state machine are still interleaved in the facade. Real-device smoke testing is now confirmed possible on Android (2026-08-13: a debug APK built, installed, and ran clean on an emulator — see `docs/BUILDING.md`'s "Real-device smoke testing" section) — Windows desktop specifically remains blocked here for a confirmed, specific reason (a Flutter-SDK/Visual-Studio-version CMake generator mismatch, also documented there), but that's no longer the whole story for "can this be smoke-tested at all." The bigger split is still deferred, now simply because it hasn't been done yet, not because testing is impossible. |
| 2 | Queue | 🟡 In progress — manual queue (add/remove/reorder via `setQueue`/`addTrack`/`removeTrack`/`playAt`), shuffle, repeat. `AudioEngine.playNext()` (§7's "play next" vs "add to queue" distinction) is now genuinely reachable end-to-end (closed 2026-08-14): added to `PluginContext` (`packages/omnis_plugin_api`, `plugin-api-v0.14.0`) and `OmnisPluginContext`, and to the Library page's per-track menu ("Play next"/"Add to queue", alongside the existing "Edit tags"/"Rate track"/etc. entries — that menu previously had no queue actions at all). Still no queue history/snapshots or smart/rule-based continuation. |
| 3 | Recovery | ✅ Solid for Phase 1 — `PlaybackWatchdog` (stuck loading, position stall, impossible position, decoder exceptions, output loss) + `PlaybackRecovery` (reload → retry → advance → stop) + `RecoveryJournal` (crash/power-loss resume) are now wired end-to-end in `MainCore`: watchdog starts with the engine, counters reset on a healthy track start, journal saves on pause/track-change/20s-heartbeat, and a "Resume where you left off?" prompt offers restoration on launch. `RecoveryJournal.save()`/`clear()` also now serialize onto one queue — they're called from several independent unawaited sites, and two concurrent writes were racing on the same `.tmp` path. Untrusted-plugin isolation (isolates) is still future work per the plugin architecture doc. |
| 4 | Database | 🟡 In progress — still JSON-file storage, not an indexed DB. §41's "atomic writes" requirement is now real across every store that persists user-authored/derived data: `LibraryStore` (atomic + debounced, prior work), `RecoveryJournal` (atomic, this session), and `PlaylistStore`/`PlayHistoryStore` (atomic, this session) — all write to a sibling `.tmp` file and rename over the real path, so a crash mid-write can no longer corrupt/truncate any of them. §41 also calls for "corruption detection" — closed a related, more severe gap this session: `LibraryStore`/`PlaylistStore`/`PlayHistoryStore` all decoded their JSON as one bulk `.map(...).toList()`/`Map.map(...)`, so a *single* malformed record (one bad track, one bad playlist, one bad stats entry) threw, was caught by the outer try/catch, and silently wiped the *entire* store back to empty — for `PlaylistStore` specifically, that means losing every hand-curated playlist with nothing to regenerate them from. All three now decode entry-by-entry and skip only the bad one, matching the per-entry guard `PlaybackState.fromJson` already had for its queue. `RemoteTextStore` (theme/layout installs) deliberately left as-is — each install is one independent file, not shared mutable state, so the blast radius of an interrupted write is already contained. Added `lib/core/backup_service.dart` (§41/§48 "automatic backup... with validation before overwriting anything") — bundles library/playlists/play-history/recovery-journal into one zip with a manifest, and `restoreBackup()` validates the *entire* archive (manifest present + recognized version, every declared file present, every declared file's JSON syntactically valid) before writing a single byte — a bad backup touches nothing, never a partial apply. Deliberately scoped to just those four store files, not `AppSettings`/plugin credentials (that's closer to §47 "Universal export," a materially bigger, separate piece of work). Wired into Settings as a new "Backup" category (`lib/ui/settings/backup_settings_page.dart`), including the search index — "Backup Omnis"/"Restore Omnis" are now searchable and reachable from the running app, not just implemented in isolation. Restore refreshes `LibraryRepository`'s in-memory cache immediately; playlists/history/journal aren't cached the same way, so the UI is upfront that a restart is needed for those to fully take effect, rather than silently leaving stale in-memory state. Still no multi-source libraries, no SQL-like query layer, no migration system. |
| 5 | Library scanning | 🟡 In progress — local-folder `MediaScanner` scan/rescan exists (pre-existing), with mtime-based incremental rescan and per-entry directory-walk error tolerance already in place. Fixed a gap this session: a file that vanished *after* being listed but *before* being stat'd (deleted mid-scan, removable storage unmounting) threw out of `file.lastModified()` and aborted the entire scan — now skipped like any other unreadable entry. Still no filesystem watchers, scheduled scans, duplicate detection, or fingerprint-based identity (§5) — tracks are still identified primarily by path. |
| 6 | Persistence | ✅ Solid for Phase 1 — playback state now survives crash/power-loss via `RecoveryJournal` (atomic writes, staleness cutoff); library and plugin-installer writes are atomic. Settings persistence (`AppSettings`) predates this session and is unchanged. |
| 7 | OS media integration | 🟡 In progress — Windows SMTC + `audio_service` notification/lock-screen controls exist (pre-existing). Not verified this session on macOS/Linux/mobile OS media session behavior. |
| 8 | Error handling | 🟡 In progress — decoder-error auto-skip, plugin-call sandboxing, plugin-installer download/zip-bomb size limits, a per-file scan failure no longer aborting the whole library scan, and (new) `library_page.dart`'s four `barrierDismissible: false` bulk-action dialogs (enrich, analyze, auto-tag, measure durations) can no longer be permanently stranded on screen — any exception mid-batch is now caught, the dialog is always dismissed, and (for `_measureDurations`) the "already running" guard flag always resets instead of silently disabling the feature for the rest of the session. Verified the metadata-enrichment and audio-analysis providers were already internally defensive (never throw) before treating this as a real gap — the actual bug was the dialogs' own lack of a `finally`, not the providers. The spec's full failure-mode checklist hasn't been run exhaustively against every subsystem — this is audit-as-you-go, not a completed sweep. |
| 9 | Tests | 🟡 In progress, meaningfully better — 478 tests passing (`flutter test`, `flutter analyze` clean; started this session at 433). New this session: `recovery_journal_test.dart`, a `MainCore` resumable-state group, `ab_repeat_controller_test.dart` (10 tests), and — via the new `PlaybackEngine` interface + `test/fakes/fake_playback_engine.dart` — `playback_watchdog_test.dart` (13 tests: every detection path) and `playback_recovery_test.dart` (10 tests: every failure-type branch, retry-then-advance behavior, custom retry limits). The recovery system, arguably the single most safety-critical subsystem per §43 of the spec, now has direct coverage for the first time. Remaining gap: `AudioEngine` itself (queue mutation, crossfade math beyond the pure `crossfadeVolumes` function, gapless/shuffle/repeat) is still untested — it needs either a real device/CI runner or an injectable-player seam of its own. |

> **Note on Phases 2–7 below:** this repo already had a mature Omnis app
> (library/playlist/settings pages, a plugin marketplace-style installer,
> a declarative theme/layout system, an accessibility pass, ~20 bundled
> plugins in `omnis_plugins`) before these Omnis 2.0 reference docs were
> added. The blanket "Not started" statuses below predate an actual
> audit against that existing code and should not be trusted as-is —
> most items likely have *some* pre-existing coverage. They need a
> dedicated audit pass (mapping existing code to each spec item) before
> being graded individually; that audit hasn't happened yet.

## Phase 2 — Library

> **Audited 2026-08-13** (Search deeply, the rest at a "does it exist and
> roughly how complete" pass) — the blanket "Not started" rows below were
> wrong for 7 of these 8 items; most of Phase 2 already had real,
> pre-existing implementations before this tracker existed.

| # | Item | Status |
|---|------|--------|
| 10 | Search | 🟡 In progress — **was a genuine 0% gap** (confirmed: zero matches for "search"/"query" anywhere in `library_page.dart` before this session), despite §6 calling search "one of Omnis' killer features." Added `lib/core/library_search.dart` (`filterTracks`) supporting free text across title/artist/album/genre plus `artist:`/`album:`/`genre:`/`title:`/`mood:`/`year:` (exact or `1990..1999` range) qualifiers, AND-combined across terms — wired into `library_page.dart`'s search box, filtering both the list and grid view paths and distinguishing "no results for this search" from "library is actually empty." 20 unit tests on the pure filter function. Not yet done: quoted multi-word field values, `rating:`/`bpm:`/`format:`/`bitrate:`/`lyrics:`/`missing:`/`duplicate:` operators (each needs a feature/data source that doesn't exist yet), natural-language queries, and search scope beyond the Library page (no global Ctrl+K command palette per §37/§38). |
| 11 | Metadata | 🟡 Pre-existing, real — `MetadataEnrichmentPlugin` (MusicBrainz always, Last.fm optional) in `omnis_plugins`, wired into `library_page.dart`'s per-track and bulk "look up metadata" actions. One provider, not the `IMetadataProvider`-pluggable-provider-framework §13 envisions (Discogs/Deezer/Genius/etc. as swappable alternatives). |
| 12 | Artwork | 🟡 Pre-existing, real — `BaseTrack.coverArt`, Android `mediastore://` artwork via `MediaScanner`, embedded-artwork extraction in `TagEditorPlugin`, a dedicated `ArtistImagePlugin`, and a `TrackArtwork` widget used throughout the UI. No artwork-provider framework (`IArtworkProvider`, Cover Art Archive/Fanart.tv lookup) or manual/drag-drop artwork override yet. |
| 13 | Playlists | 🟡 Pre-existing, real and fairly complete — `PlaylistStore` (static playlists, atomic + per-entry-safe writes, both hardened this session), M3U/M3U8 import/export, and a full `SmartPlaylistPlugin` (rule-based). Playlist folders/groups, collaborative playlists, and XSPF/PLS import/export aren't there yet. |
| 14 | Favorites | 🟡 Pre-existing, real — `FavoritesPlugin`, wired into `library_page.dart`'s per-track and bulk actions. |
| 15 | Ratings | 🟡 In progress — was a genuine 0% gap, closed this session: `RatingsPlugin` (`Omnis-Plugins` repo, tagged `v0.5.0`, pushed) stores a 0–5 rating per track (one JSON blob, decoded per-entry defensively — same corruption-resilience lesson applied from `LibraryStore`/`PlaylistStore`/`PlayHistoryStore` this cycle), 15 tests. Wired into `library_page.dart`: a "Rate track" menu item opens a 5-star picker, and a compact star indicator appears on any row that's actually rated (mirroring `FavoritesPlugin`'s heart-icon integration exactly). `ratedAtLeast()` exists as the building block for a future `rating:>=4` search operator/smart-playlist rule — neither is wired yet (`filterTracks` is a pure `BaseTrack`-only function with no plugin access; see `library_search.dart`'s doc). No bulk "rate selected" action yet (a specific 1-5 value doesn't fit the one-tap bulk-toggle pattern Favorites uses). No widget-level UI test for the `library_page.dart` wiring — consistent with `_toggleFavorite`/`_isFavorite`, which have never had one either (this repo's Library page has no widget test file at all, see item 10's note on why). |
| 16 | History | ✅ Solid — `PlayHistoryStore` (play count, last played, position tracking for Continue Listening), hardened significantly this session (atomic writes, per-entry decode safety, concurrency serialization fixing a real lost-update race). Recently Played/Most Played/Continue Listening all work off it. |
| 17 | Tag editor | 🟡 Pre-existing, real and fairly capable — `TagEditorPlugin` (ID3v1/v2, Vorbis Comments, MP4 — see its own module for the exact format list) + `tag_editor_dialog.dart`, batch auto-tag/re-tag actions in `library_page.dart`. Undo/backup/restore and a guided "Music Library Cleanup" report (§20's "1,421 missing artwork..." style analysis) aren't there yet — `library_page.dart` has a narrower duplicate/short-track cleanup tool today. |

## Phase 3 — Audio

> **Audited 2026-08-13.** Full findings in [OMNIS_2_0_MISSED_DEEP_PHASE.md](OMNIS_2_0_MISSED_DEEP_PHASE.md#phase-3--audio).

| # | Item | Status |
|---|------|--------|
| 18 | DSP pipeline | 🟡 Partial — a flat named-multiplier gain-composition system (`AudioEngine.setGainContribution`), not the spec's staged, independently-replaceable chain. Zero compressor/limiter/crossfeed/convolver/spatializer/room-correction anywhere in either repo. |
| 19 | ReplayGain | 🟡 Partial — `ReplayGainPlugin` applies gain from tags already written by an external tool (foobar2000 etc.) plus a user preamp; Omnis never scans/computes ReplayGain itself. |
| 20 | EQ | 🟡 Partial — real Android hardware EQ + a 3-band virtual trim everywhere else, per-*device* profiles (not per-artist/album), no parametric mode, no selectable band count. |
| 21 | Output devices | 🟡 Partial (closed 2026-08-14, was 0% for selection) — real, working **device listing** on every platform `audio_session` supports (speaker, wired headset, Bluetooth, USB, HDMI, via `AudioSession.getDevices()`/`devicesChangedEventStream`, no new native code — `audio_session` is already a `just_audio` dependency), plus real, working **device selection on Android 31+** (`AudioManager.setCommunicationDevice()`, exposed via `audio_session`'s `AndroidAudioManager`) reached through a new Settings → Playback & Audio → "Output devices" page (`lib/ui/settings/output_devices_page.dart`, `lib/core/output_device_controller.dart`). Below API 31, or off Android, selection degrades to an explicit "not supported here" message rather than silently failing — the list itself still works everywhere. Still missing: no USB DAC sample-rate/bit-depth control, no per-device volume/DSP profile beyond `EqualizerPlugin`'s existing per-*device-name* EQ keying (item 20). Not exercised against real hardware — same caveat as `BluetoothPlaybackPlugin`. |
| 22 | Bit-perfect | 🟡 Partial for the informational half only — `BaseTrack` now carries real `codec`/`sampleRateHz`/`bitDepth`/`bitrateKbps`/`channels` fields (§18 — `plugin-api-v0.6.0`), populated by a new pure-Dart `AudioFormatReader` that parses real file headers (FLAC STREAMINFO, WAV fmt chunk, MP3 frame header incl. Xing/VBR average bitrate) — not guessed from the extension. Surfaced via a new "Audio info" dialog on each track (`library_page.dart`). Still **0% on the DSP/output half**: no source→DSP→output *chain* display, no exclusive/WASAPI output mode, and format detection itself is honest-but-partial — M4A/AAC, OGG/Opus, WMA, AIFF get a codec label from their extension only, not real parsed sample-rate/bit-depth/bitrate (full box/page-level parsing for those containers is real, separate work). |
| 23 | Audio analysis | 🟡 Partial — `AudioAnalysisPlugin` gets real audio-derived BPM/key/mood/genre from a self-hosted Essentia service (not bundled, user must deploy it); no loudness/dynamic-range measurement, no on-device/offline analysis. |

## Phase 4 — Plugin platform

> **Audited 2026-08-13.** Full findings in [OMNIS_2_0_MISSED_DEEP_PHASE.md](OMNIS_2_0_MISSED_DEEP_PHASE.md#phase-4--plugin-platform). This phase is the most under-credited by the old blanket tracker — most of it is real.

| # | Item | Status |
|---|------|--------|
| 24 | Capability interfaces | ✅ Solid — a real typed `ServiceRegistry` (`register`/`get<T>`/`getAll<T>`, multi-provider support) with 9 interfaces (`ILyricsProvider`, `IMetadataProvider`, `IQueueBuilder`, etc.), used throughout. External (downloaded/sandboxed) plugins can now register as providers for 4 of the 9 — `lyrics`, `queue_builder`, and, added 2026-08-13, `play_history` and `artist_image` (`SandboxedPlayHistoryProvider`/`SandboxedArtistImageProvider` in `plugin_sandbox_services.dart`, wired through `PluginManager._registerProvidedServices`). The remaining 5 stay deliberately unbridged: `IMetadataProvider`/`IFileTagWriter`/`IAudioAnalysisProvider` write results back into real library storage or real audio files (a materially bigger trust decision, left for its own review), and `IVisualizerProvider` needs a genuine live `Stream` that the synchronous `callHook` request/response shape can't cleanly bridge without new polling-`Timer` + disposal machinery. |
| 25 | Plugin lifecycle | ✅ Solid mechanics — real sandboxing (`PluginSandbox.run`, catches + 8s-timeouts every hook, logs health, never propagates), a real interpreted sandbox (`dart_eval`) for downloaded plugins. Gap: no formal state-machine enum (two booleans instead), and `dart_eval` runs on the same thread (documented in `docs/PLUGIN_SECURITY.md`) so a non-yielding downloaded-plugin loop can still hang the UI. |
| 26 | Dependency resolution | 🟡 Partial for *bundled* plugins only (`requiresSequentialInit` + a two-round init) — **manifests have no `dependencies:` field parsed at all**, no version-range enforcement (`minOmnisVersion` is parsed but never read anywhere), no detection/handling of a dependency disappearing. |
| 27 | Permissions | ✅ Solid — declared manifest permissions map to real `dart_eval` grants, shown in plain English before any plugin code runs. Gap: coarse categories (`network`, `storage`), not the spec's granular `network:musicbrainz`-style scoping; `network` itself is declarative-only, not technically enforced. |
| 28 | Plugin health | 🟡 Partial — real, reactive health records (created whenever a sandboxed call throws/times out) with a live UI section on the Plugins page. Gap: no heartbeat (a silently-hung plugin isn't detected), no per-plugin retry/reset action, no dedicated health-center page. |
| 29 | Plugin updates | 🟡 Partial (was genuine 0%, closed 2026-08-13) — real update detection: `PluginInstaller.fetchRemoteManifest` fetches just the published `omnis_plugin.yaml` via `raw.githubusercontent.com` (not a full zip download), `PluginManager.checkForUpdates()` compares it against every installed external plugin's version with a real numeric (not lexical) semver-style comparison, `updatePlugin()` re-downloads and replaces it while preserving the user's enabled/disabled choice. Wired into a "Check for updates" button + per-plugin "Update available" banner on the Plugins page. Still gaps: no backup-before-update (a bad update isn't reversible), no rollback, no automatic/background checking (user-initiated only), and update detection only works for a GitHub `tree`/bare-repo source URL — a plugin installed from a direct `.zip` link can't be checked this way. |
| 30 | Marketplace/catalog | 🟡 Partial — a real permission-confirmation install flow exists, but it's "paste a GitHub URL" plus one hardcoded catalog entry, not a fetched `catalog.json`/browsable store. `docs/PLUGIN_SECURITY.md` states this outright: "No plugin registry or curation today." |

## Phase 5 — Connectivity

> **Audited 2026-08-13.** Full findings in [OMNIS_2_0_MISSED_DEEP_PHASE.md](OMNIS_2_0_MISSED_DEEP_PHASE.md#phase-5--connectivity).

| # | Item | Status |
|---|------|--------|
| 31 | OpenSubsonic | 🟢 Solid but unverified (was genuine 0%, closed 2026-08-13) — new `OpenSubsonicPlugin` (`Omnis-Plugins`) is a real client to the OpenSubsonic/Subsonic REST API: token-based auth (`t = md5(password+salt)`, never sends the plaintext password as a query param), `ping.view` connection testing, `search3.view` search returning genuinely playable `BaseTrack`s (`type: TrackType.subsonic`, real `streamUrl` pointing at the server's own `stream.view` endpoint — plays through the existing engine with zero special-casing, unlike Spotify's DRM-blocked metadata-only imports). Settings UI (server/username/password + "Test connection" + inline search-and-play) reached via the Plugins page's own settings slot, the same pattern Spotify/YouTube import use. 17 tests against a mocked HTTP client, including one that independently recomputes the MD5 token from the salt actually sent to prove the auth math is right. **Not exercised against a real Navidrome/Subsonic/Airsonic server** — protocol-level correctness only, the same caveat already applied to Spotify/YouTube. |
| 32 | Navidrome | 🟢 Solid but unverified — Navidrome implements the OpenSubsonic API, so item 31's `OpenSubsonicPlugin` already works against it in principle; no Navidrome-specific code exists or is needed. Same unverified-against-a-real-server caveat as item 31. |
| 33 | Jellyfin | 🟢 Solid but unverified (was genuine 0%, closed 2026-08-13) — new `JellyfinPlugin` (`Omnis-Plugins`) is a real client to Jellyfin's REST API: session-token auth via `/Users/AuthenticateByName` (kept in memory only, transparently re-authenticated once on a `401`), `/Items` search returning genuinely playable `BaseTrack`s (new `TrackType.jellyfin` on `plugin-api-v0.9.0`, a real `streamUrl` at Jellyfin's own `/Audio/{id}/stream` endpoint — plays with zero `AudioEngine` special-casing). Same settings-slot UI pattern as `OpenSubsonicPlugin`/Spotify/YouTube import. 17 new tests against a mocked HTTP client, including one proving the 401-retry logic is bounded to exactly one retry rather than looping forever against a server that keeps rejecting. **Not exercised against a real Jellyfin server** — protocol-level correctness only, same caveat as items 31/32/36/37. |
| 34 | Plex | 🟢 Solid but unverified (was genuine 0%, closed 2026-08-13) — new `PlexPlugin` (`Omnis-Plugins`) is a real client to Plex Media Server's REST API: `X-Plex-Token` header auth (the token itself pasted in by the user, not obtained via the full `plex.tv` sign-in/PIN flow — a deliberate, documented scope cut, the standard entry point every third-party Plex client starts from), `/search` returning genuinely playable `BaseTrack`s (new `TrackType.plex` on `plugin-api-v0.10.0`, real `streamUrl` built from the nested `Media[0].Part[0].key` — plays with zero `AudioEngine` special-casing). Plex's `/search` mixes every media type in one response; only `"type": "track"` entries are kept. 18 new tests against a mocked HTTP client. **Not exercised against a real Plex server** — protocol-level correctness only, same caveat as items 31/32/33/36/37. |
| 35 | DLNA/UPnP | 🟡 Partial (was genuine 0%, closed 2026-08-13) — new `DlnaPlugin` (`Omnis-Plugins`) is a real SSDP+SOAP/DIDL-Lite client: UDP multicast `M-SEARCH` discovery (behind an injected `SsdpTransport` so the protocol logic is fully unit-tested without a real socket), device-description XML parsing (`friendlyName`, `ContentDirectory` service `controlURL`, honoring an explicit `URLBase` when present), a real `ContentDirectory#Browse` SOAP call, and DIDL-Lite parsing into folders (containers) + genuinely playable `BaseTrack`s (new `TrackType.dlna` on `plugin-api-v0.12.0`, `streamUrl` = the real `<res>` URL, no auth needed on a trusted local network — unlike every other server type this session added). Folder-navigation UI (discover → open a server → browse into folders → play a track), not a search box — DLNA's optional `Search` action isn't consistently implemented across servers, `Browse` is the only universally-supported one. 16 new tests, including one that caught and fixed a real bug during development: `friendlyName`/`URLBase` live under `<root><device>`, not as direct children of `<root>` — a naive direct-children-only lookup silently fell back to the server's IP instead of its real name. **A real, documented, unverified-in-this-environment limitation, not just "unverified against a live server"**: Android filters incoming WiFi multicast by default, and receiving SSDP responses normally needs an acquired `WifiManager.MulticastLock`, which this plugin does not implement (would need a platform channel) — discovery may find nothing on some real Android devices even with a real, reachable DLNA server present, despite the protocol logic itself being correct and fully tested. |
| 36 | Spotify | 🟢 Solid, unverified — real PKCE OAuth (`SpotifyAuth`), playlist import (`SpotifyImportPlugin`), and Spotify-Connect remote control (`SpotifyPlaybackPlugin`), all deliberately metadata/remote-only (DRM prevents local decode — by design, not a shortfall). Self-flagged ⚠️ in `Omnis-Plugins/README.md`: never exercised against a real account. |
| 37 | YouTube | 🟢 Solid, unverified — same shape as Spotify: OAuth (`YoutubeAuth`), playlist import + public search (`YoutubeMusicImportPlugin`), and playback via YouTube's own embedded IFrame player (`YoutubePlaybackPlugin`), deliberately not stream-extraction (ToS). Self-flagged ⚠️: never run on a real device/OAuth client. |
| 38 | Other providers | 🟡 Partial (closed 2026-08-14 for Emby, was genuine 0%) — new `EmbyPlugin` (`Omnis-Plugins`) closes the one provider in this item's list with an open, self-hostable, keyless-registration server: real REST client to a self-hosted Emby server (`TrackType.emby` on `plugin-api-v0.13.0`), directly playable `streamUrl`s, same settings-slot UI pattern as OpenSubsonic/Jellyfin/Plex. Deliberately not implemented as a `JellyfinPlugin` subclass/shared-client despite the two protocols being close cousins (Jellyfin forked from Emby in 2018 and still shares its `X-Emby-Authorization` header name) — kept as its own plugin since they're two different servers a user could each run, same reasoning already applied to Plex/DLNA. Still genuine 0% for Apple Music/SoundCloud/Bandcamp/Tidal/Qobuz — each requires a closed or paid developer registration this environment can't obtain, judged not tractable. `ArtistImagePlugin`'s Deezer call remains artist-photo lookup only, not a Deezer music provider. **Not exercised against a real Emby server** — protocol-level correctness only, same caveat as every other server-connectivity plugin this session. |

## Phase 6 — Discovery

> **Audited 2026-08-13.** Full findings in [OMNIS_2_0_MISSED_DEEP_PHASE.md](OMNIS_2_0_MISSED_DEEP_PHASE.md#phase-6--discovery).

| # | Item | Status |
|---|------|--------|
| 39 | Recommendations | 🟡 Partial — the "Moods" page (`_MoodsPage`) tries each registered `IQueueBuilder` in turn. Gained a second, genuinely data-driven algorithm this session (closed 2026-08-14): "Forgotten Favorites" (`QueuePresetPlugin`, alongside its existing BPM/genre presets) — real most-played tracks (via `IPlayHistoryProvider`, backed by `ScrobblePlugin`'s persisted play history) that have dropped out of the recent-plays window, one of the spec's own named algorithms. The first real consumer of play-history data for a recommendation, closing part of "favorites/ratings/history data exists but nothing consumes it." Deliberately returns empty (not the sibling presets' whole-library-shuffle fallback) when there's no real history yet — an honest "nothing to recommend," not a misleading substitute. Still missing every other named algorithm (Similar Track/Artist, Daily/Weekly Mix, Discovery, Deep Cuts, Rediscover, New Releases, Energy Flow); `FavoritesPlugin`/`RatingsPlugin` data still isn't consumed by anything; no provider-neutral recommendation framework beyond the existing `IQueueBuilder` fan-out. |
| 40 | Sonic similarity | 🟡 Partial — `AudioAnalysisPlugin` extracts real acoustic features (BPM/key/mood/genre) via Essentia, but there's no embedding/fingerprint and no similarity/distance computation anywhere — nothing does actual "find tracks that sound like this." |
| 41 | Radio | 🟡 Partial (was genuine 0%, closed 2026-08-13) — new `RadioPlugin` (`Omnis-Plugins`) is a real client to the free, keyless [Radio Browser](https://www.radio-browser.info) directory (search/top-stations/by-tag, all real HTTP calls against the real public API), converting stations to ordinary `BaseTrack`s (`type: TrackType.radio`) that play through the existing engine with zero special-casing — a station's `streamUrl` is just an Icecast/Shoutcast stream URL, the same path YouTube/Spotify tracks already use. New "Radio" tab (`lib/ui/radio_page.dart`) with search + a top-stations default view. Gaps: single fixed API mirror, not the full DNS-round-robin server discovery Radio Browser recommends at production scale; no custom/manually-added stream URL entry; no favorites/recently-played integration for stations specifically. |
| 42 | Smart playlists | 🟡 Partial — despite the name, `SmartPlaylistPlugin` is a flat mood-string substring matcher (the same engine behind Moods), not a rule-based, multi-condition, ALL/ANY/NONE engine. No `SmartPlaylistRule`/condition-group model exists anywhere. |
| 43 | AI | 🟡 Partial (was genuine 0%, closed 2026-08-13) — real `IAIProvider` capability interface added (`packages/omnis_plugin_api`), and a first real implementation: `AIPlaylistPlugin` (`Omnis-Plugins`) builds a playlist from a natural-language prompt using Anthropic's Messages API (user-supplied key, never embedded) — the spec's §21 "Playlist creation" example ("Make me a two-hour workout playlist") almost verbatim. The model is given a compact JSON summary of the real library and told to reply with only a JSON array of track ids; every returned id is checked against the library before use, so the model can never inject a track that doesn't actually exist. 14 new tests against a mocked HTTP client, including one proving a fabricated id is silently dropped and one proving a markdown-code-fence-wrapped response (a common LLM habit despite instructions) still parses. Still genuine 0% for every other AI feature the spec names as part of this "major optional ecosystem": natural language search, metadata cleanup, tagging, recommendations, a conversational library assistant, voice control, and artist-similarity discovery. Library sample sent to the model is capped at 300 tracks — a real, documented limit, not full-library coverage. **Not exercised against the real Anthropic API** — protocol-level correctness only, no live call with real spend attached. |

## Phase 7 — Advanced UX

> **Audited 2026-08-13.** Full findings in [OMNIS_2_0_MISSED_DEEP_PHASE.md](OMNIS_2_0_MISSED_DEEP_PHASE.md#phase-7--advanced-ux). Themes/layouts/car-mode/accessibility are far more built than the blanket tracker credited; TV mode, OS widgets, and general automation are genuine 0%.

| # | Item | Status |
|---|------|--------|
| 44 | Themes | 🟡 Partial — a real declarative theme engine (import from URL/file, closed-schema colors/typography/shape/motion) exists, but none of the spec's 6 named presets (Pure/Drive/Karaoke/Future/Audiophile survive by name) do — only 4 original substitutes (Classic/Midnight/Aurora/Sunset). Themes don't touch navigation/Home/Library layout as the spec's "theme = composition" concept demands — that lives in a separate system (see item 45). |
| 45 | Layout builder | 🟢 Solid for Now Playing (a real drag-and-drop visual editor, `LayoutEditorPage`, 6 bundled layouts including Car Mode/Karaoke), **0% for Home** — `home_dashboard_page.dart` is a hardcoded, non-reorderable widget with fixed sections, no "widget canvas" per spec. No sidebar customization either. |
| 46 | Car mode | 🟢 Solid — a dedicated, deliberately-minimal Now Playing layout (`CarModeLayout`) plus real GPS-speed-triggered auto-activation (`DrivingModePlugin`, documented Android background-service limitations). No separate Car *theme*, no voice control, no Android Auto/CarPlay. |
| 47 | TV mode | 🟡 Partial (was genuine 0%, closed 2026-08-13) — new `TvModeLayout` (a new selectable Now Playing layout, same extension point `CarModeLayout` already uses) delivers the UI spec's §44 requirement close to verbatim: large artwork/text/controls, real D-pad/keyboard remote navigation. Deliberately does not hand-roll key handling — Flutter's default focus traversal (already active in any `MaterialApp`, and already the mechanism a real Android D-pad's `KEYCODE_DPAD_*` events map onto) moves focus between the transport buttons on arrow-key input for free; the layout's actual job is large, individually-focusable buttons in linear order with `autofocus` on Play/Pause so a remote never lands with nothing focused. Caught and fixed two real overflow bugs, both during development before either reached a device: a vertical overflow (large fixed sizes vs. a smaller test viewport, fixed with the same `SingleChildScrollView` guard `LandscapeLayout`/`TopControlsLayout` already use), then a horizontal one at a real phone's ~360dp logical width (Play/Pause sized proportionally larger than Previous/Next without reserving the extra width budget that needs — fixed with a `LayoutBuilder`-driven proportional split solved directly from available width, not another fixed number). 4 new dedicated tests, 3 of which simulate real arrow-key/Enter events and assert real focus moved / the real callback fired. **Verified end-to-end on a real Android emulator** — selected TV Mode via Settings, started real Radio playback, sent actual `adb shell input keyevent KEYCODE_DPAD_RIGHT`/`KEYCODE_DPAD_CENTER`, and confirmed *behaviorally* (not via Android's accessibility-focus tree, which turned out not to reliably track Flutter's internal keyboard focus at all — a real, documented discrepancy): `DPAD_CENTER` toggled real playback with a visible focus ring, and `DPAD_RIGHT`+`DPAD_CENTER` genuinely skipped to a different station. Found (but left unfixed, out of scope) a separate pre-existing bug while verifying: the *Standard* layout's Now Playing screen overflows its own button row by ~4.6px on the same device. Still 0% for a from-scratch, always-on TV/leanback shell (this is one selectable Now Playing layout among several, not a distinct app mode the whole UI switches into automatically on a TV device — no Android TV `<leanback_launcher>` intent filter, no Fire TV-specific manifest entries, no auto-detection of a TV display switching layouts automatically the way §48's "Multiple UI profiles" describes for Bluetooth-triggered Car mode). |
| 48 | Accessibility | 🟡 Partial — a real, git-log-verified accessibility pass (tooltips, `Semantics` labels on custom gesture targets, `ExcludeSemantics` on decoration, widget tests asserting on the semantics tree) plus working "Reduce motion"/"Reduce transparency"/"Haptic feedback" settings, now surfaced as their own dedicated **Accessibility** settings category (`lib/ui/settings/accessibility_settings_page.dart`, closed 2026-08-13) per §45's taxonomy — previously three rows buried a few taps into Appearance & Layout with no distinct entry point. Still gaps: no high-contrast mode, no app-wide text scaling (only `lyricsTextSize`, scoped to the lyrics view specifically), **no keyboard navigation/shortcuts anywhere** (zero `Shortcuts`/`FocusTraversalGroup` usage), so §37/§38's global search/command palette also don't exist. |
| 49 | Widgets | 🟡 Partial (was genuine 0%, closed 2026-08-13) — a real, interactive Android home-screen widget (`OmnisWidgetProvider.kt` + `home_widget` package for the Dart↔native SharedPreferences bridge): live track title/artist, and Play/Pause/Next/Previous buttons wired directly to real `android.intent.action.MEDIA_BUTTON` broadcasts targeted at `audio_service`'s own already-registered `MediaButtonReceiver` — the same mechanism a Bluetooth headset's hardware buttons already use, deliberately chosen over a Flutter background-isolate callback (`home_widget`'s own documented pattern) since this app's playback engine has no supported story for a second, widget-triggered isolate. `HomeWidgetService` (new `HomeWidgetTrackSource` seam interface, mirroring `PlaybackEngine`'s existing pattern) mirrors `AudioEngine`'s track/play-state streams into the widget on every change; degrades to a harmless no-op (logged once) on any platform/environment without the plugin registered, the same "denial degrades, never blocks boot" stance `OmnisPermissions` already uses. 5 new tests (`test/home_widget_service_test.dart`, `test/fakes/fake_home_widget_track_source.dart`) cover the Dart-side glue via a mocked `home_widget` MethodChannel. **A real, caught-and-fixed native bug**: `home_widget`'s Android side transitively depends on `androidx.work`, which declares `minSdkVersion 23` and broke the manifest merge against this app's deliberately-kept `minSdkVersion 21` — fixed with the same `tools:overrideLibrary` escape hatch already used for `audify`, extended to a 3-entry, comma-separated list (`id.nabilfaris.audify,androidx.work.ktx,androidx.work`) after two wrong attempts (colon-separated syntax, then a first library-only override that missed a second transitively-required one) — safe since `HomeWidgetService` never calls the background-worker/`registerBackgroundCallback` APIs that actually need WorkManager. **A second real bug, only findable by actually pinning the widget on a device — `flutter analyze`/`flutter test` cannot exercise Android XML layout inflation at all**: the widget's layout used a plain `<View>` as a flexible spacer between buttons, which `RemoteViews` rejects outright (`Class not allowed to be inflated android.view.View` — RemoteViews only allows a fixed class allow-list); fixed by switching to `<Space>`, which is on that list. **Verification gap, documented rather than papered over**: after the fix, re-pinning the widget to confirm it visually (live text, working buttons) could not be completed — `adb shell input draganddrop`/manual `motionevent` DOWN-hold-MOVE-UP sequences against this Compose-based Pixel Launcher widget picker succeeded exactly once (which is how the `<View>` bug was found in the first place, via the resulting "Can't load widget" error) and could not be reliably reproduced across roughly a dozen further attempts with varied timing/technique — a testing-tool/gesture-automation limitation, not a reproduced app defect; the fix itself (`<View>` → `<Space>`) is the standard, well-documented resolution for that exact Android exception. Remaining gaps: no iOS widget, no lock-screen widget beyond `audio_service`'s existing notification, one fixed layout/size (no small/large variants), no widget configuration screen, and — per the verification gap above — not confirmed rendering correctly on an actual home screen after the fix, only via source/build-level reasoning. |
| 50 | Automation | 🟡 Partial — two real, working single-purpose triggers (GPS-speed → Car Mode layout; Bluetooth-connect → quick-play/EQ-preset prompt), but no general automation-rules engine, no time-based triggers, and the Bluetooth trigger doesn't switch the UI/theme the way the spec's own example describes. |

---

## Build log

| Date | Phase | Item | Notes |
|------|-------|------|-------|
| 2026-08-12 | — | Tracker created | Build tracker initialized. |
| 2026-08-12 | 1 | Recovery, Persistence | Wired `PlaybackWatchdog`/`PlaybackRecovery`/`RecoveryJournal` (built in a prior session but never connected) into `MainCore`: watchdog now actually starts, failure counters reset on a healthy track, the journal saves on pause/track-change/20s heartbeat, and `HomePage` offers a "Resume where you left off?" prompt on launch. Removed a stray leftover file (`lib/core/playback_d`, an interrupted write from the prior session). |
| 2026-08-12 | 1 | Tests | Added `test/recovery_journal_test.dart` and a resumable-state test group in `test/main_core_test.dart`. Full suite: 445 passing, `flutter analyze` clean. |
| 2026-08-12 | 1 | Playback engine | Split `AudioEngine` per §51.2: extracted `OmnisAudioHandler`/`OmnisWindowsMediaHandler` (audio_service + Windows SMTC) into `lib/core/playback_os_integration.dart` with zero behavior change, and extracted A-B repeat into `lib/core/ab_repeat_controller.dart` as a player-decoupled, unit-testable `AbRepeatController`. Added `test/ab_repeat_controller_test.dart` (10 tests). |
| 2026-08-12 | 2 | Queue | Added `AudioEngine.playNext()` — inserts right after the current track, distinct from `addTrack()` (append) per §7. Not yet exposed through `PluginContext` or a UI context menu. |
| 2026-08-12 | 1, 2 | Tests | Full suite: 455 passing, `flutter analyze` clean. |
| 2026-08-12 | 1, 9 | Playback engine, Tests | Introduced `lib/core/playback_engine.dart` (`PlaybackEngine`, §51.3 capability interface); `AudioEngine implements PlaybackEngine`; `PlaybackWatchdog`/`PlaybackRecovery` now depend on the interface instead of the concrete engine. Added `test/fakes/fake_playback_engine.dart`, `test/playback_watchdog_test.dart` (13 tests), `test/playback_recovery_test.dart` (10 tests) — first direct unit coverage for the watchdog/recovery system. Full suite: 478 passing, `flutter analyze` clean. |
| 2026-08-12 | — | (checkpoint) | Committed (`c38f560`): recovery/journal wiring, resume prompt, AudioEngine split (OS integration, A-B repeat, PlaybackEngine interface), playNext(), and all associated tests. |
| 2026-08-12 | 4 | Database | Applied the same atomic-write fix (`.tmp` + rename) `LibraryStore`/`RecoveryJournal` already had to `PlaylistStore.save()` and `PlayHistoryStore._save()` — both were still doing a bare `writeAsString`, i.e. still corruptible by a crash mid-write, and `PlayHistoryStore._save()` in particular fires on every pause/track-change (not a rare event). Added atomic-write tests to `test/playlist_store_test.dart` and `test/play_history_store_test.dart`. Full suite: 480 passing, `flutter analyze` clean. |
| 2026-08-12 | 5, 8 | Library scanning, Error handling | `MediaScanner._trackForFile` now catches per-file failures (`file.lastModified()` throwing when a file vanishes mid-scan) instead of letting them propagate out of the `Future.wait` batch and abort the whole scan — the exact "one bad entry breaks everything" pattern the directory-listing stream already had a fix and regression tests for, one step later in the pipeline. Added a regression test to `test/media_scanner_test.dart`. Full suite: 481 passing, `flutter analyze` clean. |
| 2026-08-12 | 8 | Error handling | Wrapped `library_page.dart`'s bulk enrich/analyze/auto-tag/measure-durations loops in try/catch/finally — all four drive a `barrierDismissible: false` progress dialog with no prior exception safety net, so any mid-batch throw left the user stuck looking at an undismissable dialog (force-close-the-app territory), and for `_measureDurations` specifically also left `_measurementInProgress` stuck `true`, silently disabling the feature for the rest of the session. No new tests — a widget-test harness for these private, deeply-stateful dialog flows didn't already exist and standing one up was disproportionate to this fix's size; verified via `flutter analyze` + the existing suite staying green. Full suite: 481 passing. |
| 2026-08-12 | 4 | Database | Fixed a more severe instance of the same "one bad entry breaks everything" pattern: `LibraryStore`/`PlaylistStore`/`PlayHistoryStore` all decoded their persisted JSON in one bulk `.map(...)`, so a single malformed record wiped the *entire* store (library, playlists, or history) back to empty, not just that one entry — `PlaylistStore` in particular meant losing every hand-curated playlist. All three now decode per-entry, matching `PlaybackState.fromJson`'s existing per-entry guard for its queue. Added a regression test to each store's test file. Full suite: 484 passing, `flutter analyze` clean. |
| 2026-08-12 | 3, 4 | Recovery, Database | Found and fixed two related concurrency bugs while re-reading the stores just hardened above: (1) `RecoveryJournal.save()`/`clear()` had no serialization — two calls close together (a pause and the 20s heartbeat) raced on the same `.tmp` path. (2) Worse: `PlayHistoryStore.recordPlay`/`recordPosition` each do their own full load→mutate→save cycle, and `MainCore` fires both unawaited from independent stream listeners around every track change — two concurrent calls could each load the same base state and whichever saved last silently discarded the other's update (a lost play count or lost position, not just a file-write race). Both stores now serialize their operations onto one queue, so each call sees the previous one's committed result. Added regression tests exercising real (unawaited, `Future.wait`-joined) concurrency, not just sequential calls. Full suite: 487 passing, `flutter analyze` clean. |
| 2026-08-12 | 4 | Database | Added `lib/core/backup_service.dart` — one-click backup/restore of the library/playlists/play-history/recovery-journal, per §41/§48's "automatic backup... with validation before overwriting anything." `restoreBackup()` validates the entire archive (manifest present + recognized version, every declared file present, every declared file's JSON well-formed) before writing anything; a bad backup is provably all-or-nothing, verified by a dedicated test that a *valid* file declared alongside a *corrupt* one still gets rejected and written nowhere. 12 new tests in `test/backup_service_test.dart`. Deliberately no Settings UI yet — flagged as a distinct follow-up task. Full suite: 499 passing, `flutter analyze` clean. |
| 2026-08-13 | 4 | Database | Wired backup/restore into a new Settings → Backup page (`lib/ui/settings/backup_settings_page.dart`), including the search index — genuinely reachable and searchable in the running app, not just a service with tests. Restore refreshes `LibraryRepository`'s in-memory cache; other stores need a restart, and the UI says so rather than pretending the change is fully live. Found and fixed a real bug while wiring this up: `_restore()` called `FilePicker.platform.pickFiles()` *outside* its try/catch, so a real picker failure (permission denied, plugin unavailable) would have thrown uncaught instead of failing soft like `_backup()` already did — now the whole method is one try/catch. **Testing note**: discovered mid-session that `file_picker`'s Windows implementation can attempt a real native dialog even under `flutter test` (left genuinely hung `dart.exe` processes during investigation) — no test in this app's suite exercises `file_picker` interactively for that reason, and `backup_settings_page_test.dart` follows the same restriction (rendering-only). `BackupService` itself already has full interactive-equivalent coverage via direct calls, so the risky UI path doesn't lose real safety-net coverage. Full suite: 502 passing, `flutter analyze` clean. |
| 2026-08-13 | — | (phase transition) | Moved from "keep hardening Phase 1" to Phase 2 (Library) at the user's direction. Did a real audit of all 8 Phase 2 items before picking a starting point (see the Phase 2 table above) rather than guessing — found Search was a genuine, confirmed 0% gap despite the spec calling it a "killer feature," while most of the rest (Metadata/Artwork/Playlists/Favorites/History/Tag editor) already had real pre-existing implementations the blanket tracker never credited. Ratings is also a genuine 0% gap but needs a new plugin in the separate `Omnis-Plugins` repo plus a version bump here — deliberately deferred as a distinct, larger unit of work rather than folded into this session. |
| 2026-08-13 | 10 | Search | Added `lib/core/library_search.dart` (`filterTracks`) — free text across title/artist/album/genre, plus `artist:`/`album:`/`genre:`/`title:`/`mood:`/`year:` (exact or range) qualifiers per §6, AND-combined. Wired into `library_page.dart`'s search box (filters both list and grid paths; a real "no results" state distinct from "library is empty"). 20 unit tests, all passing. **Could not get an interactive widget test working**: `library_page_test.dart` hung indefinitely regardless of fix attempted (a real `AudioEngine()` building a real `AudioPlayer()` — fixed with a lightweight fake; `LibraryStore.save()`'s 500ms debounce `Timer` not firing under a plain `await` — fixed by seeding the JSON file directly; a third, unidentified hang past both of those). Removed the test file rather than ship a hang that would block every future `flutter test` run — the pure filter-logic tests plus `flutter analyze` are what's backing this feature instead. Full suite: 522 passing, `flutter analyze` clean. |
| 2026-08-13 | 15 | Ratings | User explicitly asked for the cross-repo work (build **and** push), not just build-locally. Added `RatingsPlugin` to `Omnis-Plugins` (mirrors `FavoritesPlugin`'s shape exactly: own `PluginStorage`, works unattached, 0-5 rating stored as one per-entry-defensively-decoded JSON blob), 15 tests, `flutter analyze`/`flutter test` clean there (84 passing total) — committed, tagged `v0.5.0` (annotated, matching the existing `v0.1.0`-`v0.4.0` convention), pushed to `github.com/MrIvoe/Omnis-Plugins`. Bumped this repo's `pubspec.yaml` `omnis_plugins` ref to `v0.5.0`, `flutter pub get`, registered in `bundled_plugins.dart`. Wired into `library_page.dart`: "Rate track" menu item -> 5-star picker dialog; a compact star indicator shows on any rated row. Full main-repo suite: 522 passing (unchanged — the new tests live in `Omnis-Plugins`), `flutter analyze` clean in both repos. |
| 2026-08-13 | 3-7 | (all) | User directive: stop asking, keep building autonomously through every remaining phase, auto-commit and push, write a dedicated "missed" document for gaps found but not fixed in the pass that found them. Ran 5 parallel research audits (one per remaining phase) against both repos before touching any code — same discipline as the Phase 2 audit. Result: Phase 4 (Plugin platform) turned out far more built than the blanket tracker credited (real `ServiceRegistry`, real sandboxing, real permission UI); Phase 3 (Audio)/Phase 6 (Discovery)/Phase 7 (Advanced UX) are a real mix of solid-but-narrower-than-spec pieces and genuine 0% gaps; Phase 5 (Connectivity) is almost entirely 0% except Spotify/YouTube, which are solid but self-flagged unverified against real accounts. Full findings appended to `OMNIS_2_0_MISSED_DEEP_PHASE.md`; phase tables above updated from "Not started" to real, cited statuses. |
| 2026-08-13 | 24 | Plugin platform | Closed part of the item-24 gap flagged in the Phase 4 audit: external (sandboxed) plugins could only register as providers for 2 of 9 `ServiceRegistry` capabilities (`lyrics`, `queue_builder`). Added `SandboxedPlayHistoryProvider`/`SandboxedArtistImageProvider` adapters in `plugin_sandbox_services.dart`, extended `providedCapabilityHooks`, wired both into `PluginManager._registerProvidedServices`/`_capabilityType`, added plain-English capability labels to the Plugins page. Deliberately did **not** bridge `IMetadataProvider`/`IFileTagWriter`/`IAudioAnalysisProvider` (their results get written back into real library storage or real audio files — a materially bigger trust decision than a lyrics string or a queue, left for its own review) or `IVisualizerProvider` (needs a genuine live `Stream`; the synchronous `callHook` shape can't cleanly bridge one without new polling-`Timer`/disposal machinery). 5 new tests (4 unit-level adapter tests, 1 end-to-end manifest→registration test) in `test/plugin_system_test.dart`; caught and fixed a test bug along the way (`MapEntry` has no value `==`, so a list-equality assertion was silently comparing by identity — replaced with explicit key/value list comparisons). Full suite: 46/46 passing in this file, `flutter analyze` clean. |
| 2026-08-13 | — | (dependency fix) | Found a genuine clean-clone-breaking bug while about to bump `omnis_plugin_api` again: this app's own `pubspec.yaml` pinned it to `plugin-api-v0.4.0` while `Omnis-Plugins` (a transitive dependency via `omnis_plugins`) already pinned the same git package to `plugin-api-v0.5.0` — pub refuses to resolve two different refs for one git source into a single package, so `flutter pub get` on a clean clone (no `pubspec_overrides.yaml`, i.e. CI or any other contributor) failed outright with a version-solving error. Local dev never surfaced this because `pubspec_overrides.yaml` unconditionally overrides `omnis_plugin_api` to a local path. Verified the failure and the fix by temporarily removing `pubspec_overrides.yaml` and running `pub get` before and after bumping the ref. Fixed by bumping `pubspec.yaml`'s ref to `plugin-api-v0.5.0`. |
| 2026-08-13 | 22 | Audio | Closed the informational half of item 22's genuine-0% gap: added real `codec`/`sampleRateHz`/`bitDepth`/`bitrateKbps`/`channels` fields to `BaseTrack` (`packages/omnis_plugin_api`, tagged `plugin-api-v0.6.0`) and a new pure-Dart `AudioFormatReader` (`lib/core/audio_format_reader.dart`) that parses real file headers — FLAC STREAMINFO (exact sample rate/bit depth/channels, average bitrate from file-size/duration), WAV fmt chunk (exact, byte-rate-derived bitrate), MP3 frame headers (sample rate/channels from the first valid frame, average bitrate preferring a Xing/Info VBR header's true frame/byte counts over the misleading first-frame value). M4A/AAC/OGG/Opus/WMA/AIFF get a codec label from their extension only — full container-level parsing for those is real, separate work, deliberately not attempted to avoid shipping wrong numbers. Wired into `MediaScanner._trackFromFile` (runs concurrently with the existing tag read) and surfaced via a new "Audio info" dialog on each track's menu in `library_page.dart`. Found and fixed a second, unrelated bug along the way: same clean-clone `omnis_plugin_api` ref problem as the previous log entry, now recurring at `v0.6.0` — this repo's `pubspec.yaml` bumped in the same commit as the tag. 15 new tests (`test/audio_format_reader_test.dart`, built against independently-hand-packed header bytes, not just round-tripping the reader's own logic) plus 2 `BaseTrack` JSON round-trip tests in `plugin_system_test.dart`. Found (but deliberately left alone, out of scope) a latent, harmless bug while writing the round-trip test: `BaseTrack.==`/`hashCode` compare `artists`/`genres` with plain `List.==`, not deep equality — documented in `OMNIS_2_0_MISSED_DEEP_PHASE.md`. Still 0% on the DSP/output half of item 22 (no source→DSP→output chain display, no exclusive/WASAPI mode). Full suite: 541 passing, `flutter analyze` clean. |
| 2026-08-13 | 41 | Discovery | Closed item 41's genuine-0% gap: new `RadioPlugin` (`Omnis-Plugins`) is a real client to the free, keyless Radio Browser directory API — `searchStations`/`topStations`/`stationsByTag`, real HTTP calls, per-entry defensive JSON decoding. Added `TrackType.radio` to `BaseTrack` (`plugin-api-v0.7.0` — a station has no fixed duration and no per-track identity, unlike `spotify`/`youtube`). Playback needed zero `AudioEngine` changes: a station is just a `BaseTrack` with a real `streamUrl`, the same path YouTube/Spotify tracks already use. New "Radio" bottom-nav tab (`lib/ui/radio_page.dart`): search + a top-stations default view; tapping a result sets the queue at that station and plays. Caught and fixed a real bug while writing tests: the `bytag` endpoint's tag was run through `Uri.encodeComponent` and then handed to `Uri.https`'s already-encoding path parameter — double-encoding any multi-word tag into literal `%2520`, silently zero-results for e.g. "80s hits." 13 new tests in `Omnis-Plugins/test/radio_plugin_test.dart`, 5 more in `test/radio_page_test.dart`. Gaps (full detail in `OMNIS_2_0_MISSED_DEEP_PHASE.md`): single fixed API mirror, no custom stream URL entry, and a real half-integration bug found along the way — a played station's history entry is genuinely recorded by `PlayHistoryStore` but never rendered on Home's "Recently Played" because that view joins history against the persisted library, which stations (fetched live, never scanned) are never part of. `Omnis-Plugins` suite: 97 passing. Main-repo full suite: 546 passing, `flutter analyze` clean in both repos. |
| 2026-08-13 | 48 | Advanced UX | Closed part of item 48's gap: moved "Reduce motion"/"Reduce transparency"/"Haptic feedback" out of Appearance & Layout into a new dedicated **Accessibility** settings category (`lib/ui/settings/accessibility_settings_page.dart`) per §45's taxonomy, which lists Accessibility as its own top-level entry alongside Appearance/Playback/Library/etc., not a sub-section. Pure relocation — same `AppSettings` properties, same behavior, only *where* they're surfaced changed (plus the search index's `category` field for each). Found and fixed a real regression caused by the new category card: it pushed "Plugins"/"Backup" past the Settings home list's `ListView` sliver cache extent on the default test viewport, breaking 3 pre-existing widget tests in `settings_page_test.dart` — fixed using the same `dragUntilVisible`/`ensureVisible` pattern the file's own "Plugins" test had already established for this exact situation (a real change to on-screen layout on a small device, not just a test-only issue). 2 new tests (category navigation + a "Reduce motion" search-deep-link test proving the search index followed the move). Full suite: 548 passing, `flutter analyze` clean. Still 0% on high-contrast mode, app-wide text scaling, and keyboard navigation/shortcuts — full detail in `OMNIS_2_0_MISSED_DEEP_PHASE.md`. |
| 2026-08-13 | 29 | Plugin platform | Closed item 29's genuine-0% gap: added `PluginInstaller.fetchRemoteManifest` (fetches just the published manifest via `raw.githubusercontent.com`, not a full zip download), a new `lib/core/semver.dart` numeric version comparator, `PluginManager.checkForUpdates()`/`updatePlugin()`, and a "Check for updates" button + per-plugin "Update available" banner on the Plugins page. Designed `updatePlugin` deliberately not to reuse `disablePlugin()` for its old-instance teardown — that method persists "disabled" to `AppSettings`, which would have silently left a previously-enabled plugin disabled after every update. 24 new tests in `plugin_installer_test.dart`/`plugin_updates_test.dart`/`plugins_page_test.dart`, 8 more in a new `semver_test.dart`. Hit and fixed the same fake-async-zone widget-test hang this session's history already documents for real (MockClient-backed) network calls inside `testWidgets()` — confirmed via a genuinely hung `flutter_tester.exe` process, fixed with the established `tester.runAsync()` + real-delay `_settle()` pattern `home_dashboard_page_test.dart` already uses for the same underlying cause. Full suite: 573 passing, `flutter analyze` clean. Gaps (full detail in `OMNIS_2_0_MISSED_DEEP_PHASE.md`): no backup-before-update/rollback, no automatic checking, and update detection only works for GitHub `tree`/bare-repo source URLs, not a direct `.zip` link. |
| 2026-08-13 | — | (verification) | User asked for a real test APK. Built one (`flutter build apk --debug`), installed it on a real Android emulator (`Medium_Phone_API_36.1`), and smoke-tested it: onboarding renders, all six bottom-nav tabs including this session's new Radio tab show up, and Radio's "Top stations" list genuinely round-trips a live call to the real Radio Browser API — not a mock. Zero errors in `adb logcat` across the session. First real-device verification any UI work in this repo has had. Also tested `flutter build windows` to check whether the "Windows build is blocked" note scattered across these docs was still accurate — it is, but for a narrower, now-confirmed reason: the pinned Flutter SDK (3.27.4) predates the installed Visual Studio Build Tools 2026 and falls back to a CMake generator string that doesn't exist on this machine. Documented both findings in `docs/BUILDING.md` and corrected the two places that cited the old, broader "can't smoke-test at all" reasoning. |
| 2026-08-13 | 31, 32 | Connectivity | Closed items 31/32's genuine-0% gap: new `OpenSubsonicPlugin` (`Omnis-Plugins`) is a real client to the OpenSubsonic/Subsonic REST API — token-based auth (`t = md5(password+salt)`, never sends the plaintext password as a query param), `ping.view` connection testing, `search3.view` search returning genuinely playable `BaseTrack`s (new `TrackType.subsonic` on `plugin-api-v0.8.0`, real `streamUrl` pointing at the server's own `stream.view` endpoint — plays with zero `AudioEngine` special-casing, unlike Spotify's DRM-blocked metadata-only imports). Since Navidrome/Airsonic both implement the same protocol, this one client closes item 32 too with no Navidrome-specific code. Settings UI (server/username/password + "Test connection" + inline search-and-play) reached via the Plugins page's existing per-plugin settings slot, same pattern Spotify/YouTube import already use — no Core changes, no new nav tab. 17 new tests against a mocked HTTP client, including one that independently recomputes the MD5 token from the salt actually sent to prove the auth math is right, not just that some token was sent. `Omnis-Plugins` suite: 114 passing. Main-repo full suite: 573 passing, `flutter analyze` clean in both. **Not exercised against a real Navidrome/Subsonic/Airsonic server** — protocol-level correctness only, the same caveat already applied to Spotify/YouTube. Jellyfin/Plex/DLNA-UPnP (items 33-35) remain genuine 0% — each is a distinct, incompatible protocol, no free coverage from this client. |
| 2026-08-13 | 33 | Connectivity | Closed item 33's genuine-0% gap: new `JellyfinPlugin` (`Omnis-Plugins`) is a real client to Jellyfin's own REST API — a deliberately separate plugin from `OpenSubsonicPlugin` (session-token auth via `/Users/AuthenticateByName`, not OpenSubsonic's per-request MD5 token; different JSON shapes; `RunTimeTicks` needs a /10,000,000 unit conversion OpenSubsonic doesn't). `/Items` search returns genuinely playable `BaseTrack`s (new `TrackType.jellyfin` on `plugin-api-v0.9.0`, real `streamUrl` at Jellyfin's own `/Audio/{id}/stream` endpoint). The session token is kept in memory only and transparently re-authenticated exactly once on a `401` — deliberately bounded (a `retried` flag through a private recursive helper) so a server that keeps rejecting every request can't cause an infinite retry loop; a dedicated test asserts exactly 2 auth calls and 2 search calls against a mock server that always returns 401, not an unbounded number. Same settings-slot UI pattern as OpenSubsonic/Spotify/YouTube import. 17 new tests. `Omnis-Plugins` suite: 131 passing. Main-repo full suite: 573 passing, `flutter analyze` clean in both. **Not exercised against a real Jellyfin server** — protocol-level correctness only, same caveat as items 31/32/36/37. Plex/DLNA-UPnP (items 34-35) remain genuine 0% — Plex is its own protocol (`plex.tv` sign-in, `X-Plex-Token`), DLNA/UPnP is SSDP+SOAP, not a REST API at all, so neither gets free coverage here either. |
| 2026-08-13 | 34 | Connectivity | Closed item 34's genuine-0% gap: new `PlexPlugin` (`Omnis-Plugins`) is a real client to Plex Media Server's REST API — a third, separate plugin from OpenSubsonic/Jellyfin (account-scoped `X-Plex-Token` header on every request, not a computed per-request token or a login-issued session; `/search` mixes every media type in one response, filtered here to `"type": "track"` only; artist lives on `grandparentTitle`, duration is milliseconds needing a `~/1000` conversion, the real stream path is nested three levels down at `Media[0].Part[0].key`). Deliberately does not implement Plex's full `plex.tv` sign-in/PIN-pairing flow — the user pastes in a token they already have, the standard entry point every third-party Plex client starts from; a documented, narrower scope than OpenSubsonic/Jellyfin's direct username/password. New `TrackType.plex` on `plugin-api-v0.10.0`; real `streamUrl` at the actual file Plex serves, zero `AudioEngine` special-casing. Same settings-slot UI pattern as the other two server plugins. 18 new tests, including one proving the type-filter correctly drops mixed album/artist/movie results and one proving a track with no `Media`/`Part` is skipped rather than producing a broken stream URL. `Omnis-Plugins` suite: 149 passing. Main-repo full suite: 573 passing, `flutter analyze` clean in both. **Not exercised against a real Plex server** — protocol-level correctness only, same caveat as items 31/32/33/36/37. Item 35 (DLNA/UPnP) remains genuine 0% — a fundamentally different kind of protocol (SSDP+SOAP, not REST), no free coverage from any of the three server plugins now built. |
| 2026-08-13 | 43 | AI | Closed one deliberately narrow slice of item 43's genuine-0% gap: real `IAIProvider` capability interface (`packages/omnis_plugin_api`) — genuinely async, unlike `IQueueBuilder.buildQueueFor`'s synchronous contract, since a real AI provider needs a network round-trip that interface can't accommodate. First implementation `AIPlaylistPlugin` (`Omnis-Plugins`) calls Anthropic's real Messages API (user-supplied key, never embedded) for the spec's §21 headline example — "Make me a two-hour workout playlist." The model gets a compact JSON summary of the real library and must reply with only a JSON array of ids; every returned id is checked against the library before use, so a fabricated/invented id is silently dropped, never surfaced as a broken track — the load-bearing guarantee, directly tested. Library sample capped at 300 tracks (documented, real limit); a markdown-code-fence-wrapped response (a common LLM habit despite instructions) is stripped defensively before parsing, also tested. 14 new tests against a mocked HTTP client. `Omnis-Plugins` suite: 163 passing. Main-repo full suite: 573 passing, `flutter analyze` clean in both. Still genuine 0% for every other AI capability the spec names (natural-language search, metadata cleanup, tagging, the §22 recommendation engine, a conversational library assistant, voice control, artist-similarity discovery) — full detail in `OMNIS_2_0_MISSED_DEEP_PHASE.md`. **Not exercised against the real Anthropic API** — protocol-level correctness only, no live call with real spend attached. |
| 2026-08-13 | 35 | Connectivity | Closed item 35's genuine-0% gap: new `DlnaPlugin` (`Omnis-Plugins`) is a real SSDP+SOAP/DIDL-Lite client — UDP multicast `M-SEARCH` discovery behind an injected `SsdpTransport` (real impl `UdpSsdpTransport`, a fake in tests), device-description XML parsing (honoring `URLBase` when present), a real `ContentDirectory#Browse` SOAP call, DIDL-Lite parsing into folders + genuinely playable `BaseTrack`s (new `TrackType.dlna` on `plugin-api-v0.12.0`, no auth needed on a trusted network — unlike every other server type this session added). Needed a new dependency (`xml: ^6.5.0`, first XML parser in either repo) and a new I/O primitive (`RawDatagramSocket`, first raw UDP use in either repo). Folder-navigation UI, not a search box — UPnP's optional `Search` action isn't consistently implemented across servers. 16 new tests caught and fixed a real bug: `friendlyName`/`URLBase` live under `<root><device>`, not as direct children of `<root>` — a direct-children-only lookup silently fell back to the server's bare IP instead of its real name. **Also caught a real process gap while verifying**: adding `xml` to `Omnis-Plugins`' `pubspec.yaml` and running `pub get` *there* didn't propagate to this app — the local `pubspec_overrides.yaml` path dependency picks up source changes immediately but not new *transitive* dependencies, so the main app's own `flutter test` failed to even compile (`Error: Not found: 'package:xml/xml.dart'`) until `flutter pub get` was re-run here too; a lesson for every future increment that adds a new pub dependency in `Omnis-Plugins`, not just new source files. `Omnis-Plugins` suite: 179 passing. Main-repo full suite: 573 passing, `flutter analyze` clean in both. **A real, documented limitation more severe than "unverified against a live server"**: Android filters WiFi multicast by default, and real SSDP reception normally needs an acquired `WifiManager.MulticastLock` this plugin doesn't implement (would need a platform channel) — discovery may find nothing on real Android hardware even with a reachable server present, despite the protocol logic being correct and fully tested. Deliberately did not add the `CHANGE_WIFI_MULTICAST_STATE` manifest permission alone without the matching lock-acquisition code — full detail in `OMNIS_2_0_MISSED_DEEP_PHASE.md`. |
| 2026-08-13 | 47 | Advanced UX | Closed part of item 47's genuine-0% gap: new `TvModeLayout` — large artwork/text/controls, real D-pad/keyboard remote navigation per the UI spec's §44 "TV" requirement, added as a new selectable Now Playing layout via the exact extension point `CarModeLayout` already established. Deliberately doesn't hand-roll key handling — Flutter's default focus traversal already moves focus on arrow-key input, and a real Android D-pad's `KEYCODE_DPAD_*` events already arrive at Flutter as those same logical arrow keys, so the actual job was large individually-focusable buttons in linear order with `autofocus` on Play/Pause. Caught and fixed a real overflow bug during development (large fixed sizes overflowed the default test viewport) with the same `SingleChildScrollView` guard `LandscapeLayout`/`TopControlsLayout` already use. 4 new tests, including 3 that simulate real arrow-key/Enter key events and assert the actual `FocusNode`/callback moved/fired, not just that something rendered. Full suite: 577 passing, `flutter analyze` clean. Still genuine 0% for a from-scratch always-on TV/leanback shell (no Android TV launcher intent filter, no automatic TV-display detection switching layouts, no TV-specific navigation for any screen other than Now Playing) — full detail in `OMNIS_2_0_MISSED_DEEP_PHASE.md`. |
| 2026-08-13 | 47 | Advanced UX | Real-device verification pass on the TV Mode work above caught a *second*, more subtle overflow the first fix and the 800px-wide widget-test default both missed: at a real phone's ~360dp logical width, the button row itself overflowed 17px horizontally, because sizing Play/Pause proportionally larger than Previous/Next without reserving the extra width its larger size needs broke the row's total-width budget. Fixed by solving each button's size directly from the real available width (`LayoutBuilder`-driven proportional split) instead of another fixed guess. Added a dedicated test at a real narrow width (360×800) to catch this class of bug going forward — full suite now 578 passing. Then verified the fix **end-to-end on the real Android emulator**: selected TV Mode via Settings, started real Radio playback (MANGORADIO), and drove it with actual `adb shell input keyevent KEYCODE_DPAD_RIGHT`/`KEYCODE_DPAD_CENTER` — the first genuine real-device D-pad verification any UI work in this repo has had. Confirmed *behaviorally*, not via Android's accessibility-focus tree (`uiautomator dump`'s "focused" attribute stayed frozen across a real `DPAD_RIGHT` press that widget tests independently prove moves Flutter's internal focus — not a reliable oracle for this, a real finding worth knowing for future on-device focus checks): `DPAD_CENTER` toggled real playback (pause icon → play icon, with a visible focus-highlight ring around the button), and `DPAD_RIGHT` + `DPAD_CENTER` genuinely skipped to a different station (MANGORADIO → REYFM), proving both that focus moved to Next and that activating it worked for real. Found and documented (not fixed, out of scope) a separate pre-existing bug while verifying: the *Standard* layout's own Now Playing screen overflows its button row by ~4.6px on this same device — full detail in `OMNIS_2_0_MISSED_DEEP_PHASE.md`. |
| 2026-08-13 | 49 | Advanced UX | Closed item 49's genuine-0% gap: new real, interactive Android home-screen widget. Added `home_widget: ^0.9.2+1`, `lib/core/home_widget_track_source.dart` (new seam interface, same pattern as `PlaybackEngine`), `lib/core/home_widget_service.dart` (mirrors `AudioEngine`'s track/play-state into the widget, degrades gracefully — logged once, not per change — anywhere the plugin isn't registered, e.g. Windows desktop or a plain `flutter_test` run), `android/app/src/main/kotlin/.../OmnisWidgetProvider.kt` (renders title/artist, wires Play/Pause/Next/Previous to real `MEDIA_BUTTON` broadcasts at `audio_service`'s existing `MediaButtonReceiver` rather than a Flutter background isolate), plus the layout/drawable/manifest resources a native widget needs. 5 new tests (`test/home_widget_service_test.dart`, `test/fakes/fake_home_widget_track_source.dart`) cover the Dart-side glue against a mocked `home_widget` channel. Hit and fixed two real native build/runtime bugs along the way — full detail in the item 49 table row above: (1) a `minSdkVersion` manifest-merge conflict from `home_widget`'s transitive `androidx.work` dependency, resolved via a 3-entry `tools:overrideLibrary` list after two wrong syntax attempts; (2) a `RemoteViews` inflation crash (`Class not allowed to be inflated android.view.View`) from using a plain `<View>` spacer, fixed with `<Space>` — found only by actually pinning the widget on the emulator, since neither `flutter analyze` nor `flutter test` can exercise Android XML layout inflation. **Honestly could not re-confirm the fix visually**: re-pinning the widget after the fix to screenshot it working failed across roughly a dozen `adb`-scripted long-press-drag attempts against the emulator's Compose-based widget picker (the one successful pin, which is how the `<View>` bug was found, could not be reliably reproduced) — documented as a testing-tool limitation, not a reopened app defect, since the fix itself is the standard resolution for that exact exception. Full suite: 583 passing, `flutter analyze` clean. Main-repo-only change, no `packages/omnis_plugin_api` bump needed. |
| 2026-08-14 | 21 | Audio | Closed the selection half of item 21's genuine-0% gap: real Output Devices settings page. Unlike the home-screen-widget increment just before this one, needed **zero new native platform-channel code** — `audio_session` (already a `just_audio` dependency) already exposes cross-platform device enumeration (`AudioSession.getDevices()`, `devicesChangedEventStream`) and, on Android 31+, real device selection via `AndroidAudioManager.setCommunicationDevice()` (the documented modern replacement for the older per-`AudioTrack` `setPreferredDevice()` — despite the "communication" name, this is what actually routes this app's playback session). New `lib/core/output_device_controller.dart` (`OutputDeviceSource` seam interface + `AudioSessionOutputDeviceSource` real implementation + a pure `classifyOutputDeviceType` mapping function, the one part of this feature testable without a platform channel) and `lib/ui/settings/output_devices_page.dart`, reached from Settings → Playback & Audio → "Output devices". Below API 31 or off Android, `selectDevice` fails soft with a clear reason rather than silently no-op'ing, and the whole selection UI greys out with an explanatory note — the device *list* itself still works everywhere `audio_session` does. 6 pure-function tests plus 7 widget tests against a `FakeOutputDeviceSource` (`test/fakes/fake_output_device_source.dart`) covering listing, selection, the unsupported-platform degrade, error surfacing, and live refresh on a devices-changed event. Caught a real test-writing mistake, not an app bug, while writing the "tap System default" test: the "System default" `RadioListTile`'s `value` (`null`) matches the initial `groupValue` (`null`) by definition, so tapping it immediately is a no-op tap on an already-selected radio — real, correct `Radio` behavior — fixed by selecting a different device first in the test before switching back. Full suite: 596 passing, `flutter analyze` clean. Still missing: no USB DAC sample-rate/bit-depth control, no per-device volume/DSP profile beyond `EqualizerPlugin`'s existing per-device-name EQ keying. **Not exercised against real hardware** — same caveat already applied to `BluetoothPlaybackPlugin` and this session's various server plugins. |
| 2026-08-14 | 38 | Connectivity | Closed part of item 38's genuine-0% gap: new `EmbyPlugin` (`Omnis-Plugins`) is a real client to a self-hosted Emby media server — session-token auth via `/Users/AuthenticateByName`, `/Items` search returning genuinely playable `BaseTrack`s (new `TrackType.emby` on `plugin-api-v0.13.0`, real `streamUrl` at Emby's own `/Audio/{id}/stream`). Emby is the one provider on this item's original list that's actually tractable here — free, self-hostable, no developer-registration gate — unlike Apple Music/SoundCloud/Tidal/Qobuz/Bandcamp, each blocked by a closed or paid developer program (SoundCloud specifically closed new API key registrations in 2023), judged a durable "not tractable from this environment" rather than a scheduling gap. Deliberately kept as its own plugin rather than sharing `JellyfinPlugin`'s client, even though the two protocols are close cousins (Jellyfin forked from Emby in 2018, still shares its `X-Emby-Authorization` header name) — same "one plugin per real server a user could independently run" reasoning already applied to Plex/DLNA despite their own occasional shape overlap. 17 new tests, generated from `JellyfinPlugin`'s own test suite via a scripted find/replace then independently verified passing (auth, search parsing, per-entry defensive decoding, the bounded-401-retry test). `Omnis-Plugins` suite: 196 passing. Main-repo full suite: 596 passing (unchanged — Emby's tests live in `Omnis-Plugins`), `flutter analyze` clean in both. Verified end-to-end on the emulator: reinstalled the rebuilt APK, confirmed `initializeAll()` now reports 29 plugins (28 + Emby), matching `bundled_plugins.dart`'s factory count, zero crashes. **Not exercised against a real Emby server** — protocol-level correctness only, same caveat as every other server-connectivity plugin this session. |
| 2026-08-14 | 2 | Reliability | Closed the reachability half of item 2's gap: `AudioEngine.playNext()` (added in an earlier session, never wired anywhere) is now genuinely usable end-to-end. Added `PluginContext.playNext(BaseTrack)` to `packages/omnis_plugin_api` (`plugin-api-v0.14.0`) and its forwarding implementation in `OmnisPluginContext` — a plugin can now insert "play next" the same way it already could `addTrack`/`removeTrack`, closing a real capability gap (before this, a plugin wanting "play next" behavior had no way to ask for it at all, only "add to queue"). Added "Play next"/"Add to queue" to the Library page's per-track `PopupMenuButton` — that menu previously had zero queue-related actions (only tag/playlist/rating/metadata/ringtone entries), so this was a genuine, user-visible gap, not just an API completeness one. New test in `test/plugin_architecture_test.dart` proves `OmnisPluginContext.playNext` forwards to the real `AudioEngine.playNext`, not silently falling back to `addTrack`/`setQueue`. Since `PluginContext` is a cross-repo shared interface, this required the full version-bump cycle: `plugin-api-v0.14.0` tagged and pushed, `Omnis-Plugins`' own ref bumped and re-tagged `v0.14.0` (no plugin code there needed the new method yet, but its pinned ref still has to move in lockstep or a clean clone fails to resolve two different refs for one git source), then this repo's own `pubspec.yaml` bumped for both packages and clean-clone resolution re-verified. Full suite: 597 passing, `flutter analyze` clean in both repos. Did **not** add new `AudioEngine`-level tests for `playNext()`'s internal queue-insertion logic itself — that's the same pre-existing, already-documented gap item 9 names (`AudioEngine` has no injectable-player test seam yet), not a new regression introduced here. |
| 2026-08-14 | 39 | Discovery | Closed part of item 39's gap: added "Forgotten Favorites" to `QueuePresetPlugin` (`Omnis-Plugins`) — the first recommendation algorithm in this app backed by real listening-history data rather than a track's own static properties. Uses `IPlayHistoryProvider` (already registered by `ScrobblePlugin`, previously consumed by nothing) to find tracks among a listener's real most-played that have dropped out of their most-recent-plays window — "things you used to love and have drifted from," one of the spec's own named algorithms (§39), picked because the data to build it honestly already existed and simply had no consumer. Deliberately does **not** reuse `buildQueue`'s existing BPM/genre presets' "fall back to a shuffled whole library when nothing matches" behavior — that fallback makes sense for an objective claim like "Workout" (a shuffled library is still *a* set of tracks in roughly that spirit), but "Forgotten Favorites" is a claim about *this listener's own history*, and a shuffled library isn't a smaller version of that claim, it's a different and misleading one — so it returns genuinely empty (surfaced as the Moods page's existing "no tracks yet" message) when there's no real history to draw from. 7 new tests in `test/queue_preset_plugin_test.dart` (in the main repo, matching where this plugin's existing tests already lived) against a fake `IPlayHistoryProvider`, covering: no context, no registered provider, a provider with empty history, the core recent-vs-most-played set difference, a most-played id no longer present in the current library (skipped, not a broken entry), and case-insensitive query matching. `Omnis-Plugins` suite: 196 passing (unchanged — new tests live in the main repo). Main-repo full suite: 604 passing, `flutter analyze` clean in both. Cross-repo version bump: `Omnis-Plugins` tagged `v0.15.0`, this repo's `omnis_plugins` ref bumped to match, clean-clone resolution re-verified (no `omnis_plugin_api` change this time, so only one ref moved). Still genuine 0% for every other named algorithm (Similar Track/Artist, Daily/Weekly Mix, Discovery, Deep Cuts, Rediscover, New Releases, Energy Flow), and `FavoritesPlugin`/`RatingsPlugin` data remains unconsumed by any recommendation. |
