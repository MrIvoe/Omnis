# Core analysis (BPM/key) needs only this — no TensorFlow.
essentia

# essentia-tensorflow (which also provides everything `essentia` does,
# plus TensorflowPredictMusiCNN for mood/genre tagging) is only published
# as a prebuilt wheel for Linux + Python 3.5-3.7 per Essentia's own docs.
# If that constraint doesn't match your environment, comment out the line
# below, keep plain `essentia` above, and the service will run with
# BPM/key only (mood/genres come back empty — see app.py's HAS_TENSORFLOW
# flag). Building essentia-tensorflow from source for a newer Python is
# covered in Essentia's own blog post, referenced from the tutorial this
# service is based on.
essentia-tensorflow; sys_platform == "linux"

fastapi
uvicorn[standard]
python-multipart
