// `FavoriteChangedEvent` moved to `omnis_plugin_api` — see
// `packages/omnis_plugin_api/lib/events.dart` for the real definition.
// Re-exported from its original path so every existing
// `import 'package:omnis/plugin_api/events.dart'` in this app keeps
// working unchanged.
export 'package:omnis_plugin_api/events.dart';
