// `ServiceRegistry` moved to `omnis_plugin_api` — see
// `packages/omnis_plugin_api/lib/service_registry.dart` for the real
// definition. Re-exported from its original path so every existing
// `import 'package:omnis/core/service_registry.dart'` in this app keeps
// working unchanged.
export 'package:omnis_plugin_api/service_registry.dart';
