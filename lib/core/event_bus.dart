// `EventBus` moved to `omnis_plugin_api` — see
// `packages/omnis_plugin_api/lib/event_bus.dart` for the real
// definition. Re-exported from its original path so every existing
// `import 'package:omnis/core/event_bus.dart'` in this app keeps
// working unchanged.
export 'package:omnis_plugin_api/event_bus.dart';
