// `PluginStorage` moved to `omnis_plugin_api` — see
// `packages/omnis_plugin_api/lib/plugin_storage.dart` for the real
// definition. Re-exported from its original path so every existing
// `import 'package:omnis/core/plugin_storage.dart'` in this app keeps
// working unchanged.
export 'package:omnis_plugin_api/plugin_storage.dart';
