// `MusicPlugin` moved to `omnis_plugin_api` — see
// `packages/omnis_plugin_api/lib/plugin_interface.dart` for the real
// definition. Re-exported from its original path so every existing
// `import 'package:omnis/core/plugin_interface.dart'` in this app keeps
// working unchanged.
export 'package:omnis_plugin_api/plugin_interface.dart';
